[project]
name = "plasma-agent-purposes-plus"
version = "0.6.0"
description = "Plasma inventory optimizer with expanded segments, purpose tuning, ML, and forecast-aware modes"
requires-python = ">=3.9"
dependencies = ["streamlit","pandas","numpy","matplotlib","pyomo","scikit-learn","statsmodels"]
