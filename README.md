# Plasma Inventory Optimizer — Purposes Plus
Expanded downstream segments (hospital, pharma, research, biotech, blood_bank, export) with purpose tuning and forecast-aware optimization.

Run:
uvx --with pandas --with numpy --with matplotlib --with pyomo --with scikit-learn --with statsmodels --with highspy streamlit run app.py
